/** @format */

import '../styles/spinner.css'; // Assurez-vous que le chemin est correct

const Spinner = () => {
    return (
        <div className="spinner-container">
            <div className="spinner"></div>
        </div>
    );
};

export default Spinner;
